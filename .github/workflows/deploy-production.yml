name: Deploy Placeholder (production)

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    name: placeholder
    runs-on: ubuntu-latest

    environment:
      name: production

    steps:
      - name: Placeholder marker
        run: echo "Placeholder only: no production deployment is executed by this workflow."
